# Website

Aero's website lives here. The folder is not a Swift target, so SwiftPM ignores it and `./x` does not
build it yet. When the site arrives, give it a `./x web` command and a workflow, as the app has.
